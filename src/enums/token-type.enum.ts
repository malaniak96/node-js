export enum ETokenType {
  // eslint-disable-next-line no-unused-vars
  ACCESS = "access",
  // eslint-disable-next-line no-unused-vars
  REFRESH = "refresh",
}

export enum EActionTokenType {
  // eslint-disable-next-line no-unused-vars
  FORGOT = "forgot",
  // eslint-disable-next-line no-unused-vars
  ACTIVATE = "activate",
}
